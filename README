This project demonstrates the use of an Observer/Subject pattern in modern PHP programming using the error handler as an example

This project is aimed for PHP training sessions and is not designed to be used as a production code as-is.

PHP >= 8.1 needed